<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vistaar">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ImageActivity"></activity>
        <activity android:name=".manaufacturing_upload" />
        <activity android:name=".manufacturing_search" />
        <activity android:name=".manufacturing1" />
        <activity android:name=".company_search" />
        <activity android:name=".company_upload" />
        <activity android:name=".Company1" />
        <activity android:name=".farmer_upload" />
        <activity android:name=".farmer_search" />
        <activity android:name=".Farmer1" />
        <activity android:name=".Field_select" />
        <activity android:name=".Register" />
        <activity android:name=".Login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>